import { style } from "@vanilla-extract/css";
import { vars } from "../../common/vars.css";

export const reportSegmented = style({
  marginBottom: vars.space.lg,
});

export const reportStats = style({
  marginBottom: vars.space.lg,
});
